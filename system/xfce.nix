{
  config = {
    services = {
      xserver = {
        desktopManager.xfce.enable = true;
        enable = true;
        layout = "us";
        xkbOptions = "caps:escape";
        xkbVariant = "";
      };
    };
  };
}
